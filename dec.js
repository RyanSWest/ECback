//  const secretKeyArray = JSON.parse(process.env.SOLANA_PRIVATE_KEY);
// const keypair = Keypair.fromSecretKey(Uint8Array.from(secretKeyArray));

const thing= process.env.SOL_SECRET_KEY1

console.log(thing)